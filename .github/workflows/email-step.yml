name: Test Email Step

on:
  push:
    branches:
      - main   # triggers when you push to main
  workflow_dispatch:   # allows manual trigger

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 23f2000261@ds.study.iitm.ac.in
        run: echo "Hello, world!"
